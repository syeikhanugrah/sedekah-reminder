{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
        <div class="offset-sm-2 col-sm-10">
            <button class="btn btn-primary btn-block-xs-only mb-2 mb-sm-0">Simpan</button>
            <a href="{{ path('user_index') }}" class="btn btn-secondary btn-block-xs-only mb-2 mb-sm-0">&laquo; Kembali ke daftar</a>
        </div>
    </div>
{{ form_end(form) }}
